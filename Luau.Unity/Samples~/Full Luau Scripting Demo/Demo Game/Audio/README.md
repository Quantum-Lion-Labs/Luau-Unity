# Optional audio

No audio clips are bundled. Assign your own clips to the scene's `Flap Audio`,
`Score Audio`, and `Hit Audio` AudioSource components. The Luau scripts remain
silent when `hasClip` is false.
